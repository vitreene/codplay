# Lot 10 tests

This folder contains the tests for:

- `L10-T1` style key conflict keeps the last action and traces override
- `L10-T2` style keys without overlap remain co-applied
- `L10-T3` attr key conflict keeps the last action and traces override
- `L10-T4` className token add/remove conflict keeps the last operation
- `L10-T5` same keys on different targets do not conflict
- `L10-T6` move-only actions are preserved and applied

Reference:

- `evolution/lots/lot-10-conflits-same-tick-runtime.md`
