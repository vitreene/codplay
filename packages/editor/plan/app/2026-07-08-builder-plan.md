# Spec + Plan — Le Builder (ed2)

**Périmètre** : nouveau composant, `packages/editor/src/builder/` (à créer). Transforme les données d'édition (`EditorScene` de grid-editor, `DecorPatch` de dedit) en `SceneDef` Codplay, envoyé au player. **Sa classe métier testable est `SceneDocEditor`** (`packages/authoring/scene-factory`) : le Builder oriente/orchestre (résolution timing/transitions/CSS), `SceneDocEditor` construit et valide le `SceneDef`.
**Dépend de** :
- `2026-07-08-capsule-automation-reconciliation-plan.md` (le Builder consomme les deux modules dans leurs rôles réconciliés).
- `2026-07-08-scenedoceditor-audit-plan.md` (`SceneDocEditor` remis à niveau face au modèle Codplay actuel — chantier distinct, préalable).

---

## Principes fondamentaux

Ces deux règles définissent ce qu'est le Builder — pas des détails locaux à une section, elles s'appliquent à tout ce qu'il produit.

### A. Eventime = déclencheur, action = données

**L'eventime envoie un event ; c'est l'action du perso qui porte les données.** L'eventime n'est qu'un déclencheur (`{name, startAt}`), jamais un porteur de payload — la seule exception légitime est quand un emit utilisateur ou un strap apporte une donnée réellement dynamique au perso, un cas qui ne se présente jamais dans ed2 (pas de strap, pas d'écoute, tout connu au build). Le Builder n'a donc **aucune raison** d'utiliser le raccourci `event.name === perso.id` / `event.data` : chaque comportement est une action **nommée et déclarée statiquement** sur le perso concerné ; l'eventime se contente de la déclencher au bon instant. Voir [[feedback-eventime-vs-action-data]].

### B. Aucune donnée inventée par le Builder

**Le Builder ne transforme et ne route que des données déjà explicites quelque part (état d'édition, catalogue de config) — il n'en invente jamais.** Si une donnée facultative est absente en entrée, elle reste absente en sortie ; pas de substitution silencieuse, pas de constante par défaut codée en dur dans le code du Builder. Une valeur codée en dur à cet endroit est invisible et impossible à tracer ou à surcharger. Si un vrai besoin de valeur par défaut apparaît à l'usage, il se résout **soit en config** (un champ explicite et nommé dans un catalogue existant, ex. capsule-automation), **soit dans l'éditeur** (un réglage auteur explicite et visible) — jamais par une ligne ajoutée dans le Builder lui-même. Voir [[feedback-no-hardcoded-defaults-in-builder]].

---

## Partie spec — ce que le Builder produit

### 1. Cible : `SceneDef`, jamais `CompiledScene`

Le Builder ed2 s'arrête à la production d'un `SceneDef` (= `SceneDoc`, même objet). La compilation `SceneDef → CompiledScene` reste la responsabilité du builder Codplay existant (`packages/codplay/src/builder/`), invoquée en aval via `CodPlay.load()`.

### 2. Une scène ed2 = une story, sans strap, sans écoute

```ts
{ id: 'story-main', name: 'main', initial: { move: '@root' }, persos: [...], straps: undefined, listen: [], eventimes: [...] }
```
`straps: undefined` + `listen: []` est une config légale (confirmé dans les specs Codplay) — pas une tolérance dégradée.

### 3. Capsule racine — toujours présente, construite en premier

Chaque `SceneDef` produit contient, en tête de `story.persos`, une capsule racine :

```ts
{
  id: 'story-main__root',
  name: 'root',
  type: 'list',                    // tous les types de capsule → list, uniformément
  initial: {
    move: '@root',                 // ⚠ ET story.initial.move doit AUSSI valoir '@root' — les deux sont exigés (deriveRootNodeIds)
    tag: 'div',
    style: { /* décor résolu de la capsule racine — voir §4 */ },
  },
  actions: {}   // aucune action : la capsule racine ne reçoit jamais d'eventime (pas de transition, décor statique posé une seule fois dans initial)
}
```

Règles (actées, cf plan général) :
- Jamais visible/sélectionnable comme item, non supprimable.
- Pas de keyframe, pas de `PositionPatch` — décor statique, défini une seule fois.
- Capsule-automation : sous-type `card` (`GRID_MODE.manual`), zéro zone définie — fond layout, placement explicite par item (`2026-07-08-capsule-spec.md` §3/§6).
- Tout item créé par l'utilisateur devient un enfant d'elle (`move:{parentId:'story-main__root', flip:false}`), jamais lui-même `@root`.

### 4. Décor de la capsule racine — gap de modèle de données à combler côté grid-editor

`EditorScene` n'a aujourd'hui aucun champ pour un décor sans keyframe. Ce chantier doit ajouter à `EditorScene` (sequence-editor) un champ dédié, ex. `rootDecorId: string | null`, pointant dans le registre `decors{}` existant — **pas** de détournement du mécanisme `Keyframe.decorId`. Cette extension de type est un pré-requis du Builder, pas une tâche du Builder lui-même — à faire dans `packages/editor/src/sequence-editor/types.ts` avant ou en même temps.

### 5. Item — perso enfant

Chaque item ed2 (texte, image, média, capsule imbriquée…) devient un perso Codplay enfant de la capsule racine (ou d'une capsule imbriquée). Mapping `ItemType` → type perso Codplay :

| ItemType (dedit) | type perso Codplay |
|---|---|
| `text` | `text` |
| `image` | `img` (⚠ pas `'image'` — piège de nom confirmé, `ResourceManifestEntry.type` utilise `'image'`, le perso utilise `'img'`) |
| `video` | `media` |
| `media` | `media` |
| `capsule` | `list` |
| `bloc` | `text`, contenu vide (pas une valeur `ItemType` distincte — cf `2026-07-08-item-model-spec.md` §5) |

**Placement d'un enfant dans une capsule `card`** : `placementPolicy: explicitOnly` (`2026-07-08-capsule-spec.md` §3) — le Builder pose donc toujours un `AutoCapsuleChildInput.placement` explicite, jamais déduit par capsule-automation. Si `DecorPatch.zone` référence une zone réelle, le Builder la traduit en `{row,col,rowSpan,colSpan}` ; si elle vaut `null`, le Builder résout vers la **zone fantôme** de la capsule (`{row:1, col:1, rowSpan: grid.rows, colSpan: grid.cols}`, `2026-07-08-capsule-spec.md` §3/§11) — le même mécanisme nom-de-zone → coordonnées, appliqué à une zone toujours présente plutôt qu'une valeur inventée au cas par cas.

### 6. Transitions intro/outro — mécanisme complet

Application directe du Principe A : chaque transition devient une action nommée et déclarée statiquement sur le perso, jamais un payload dans l'eventime.

1. Le Builder appelle `CapsuleDistribution.compute()` (`packages/authoring/scene-factory/`, co-localisée avec `SceneDocEditor`) pour obtenir le `{introMs, outroMs}` de chaque enfant d'une capsule (relatif à l'intro de la capsule).
2. Conversion en absolu (`capsule.introAbsoluteMs + introMs`), posé sur `AutoCapsuleChildInput.timeRange` (cf plan de réconciliation).
3. `AutoCapsule.resolve()` résout la transition nommée (`ref`, ex. `'fade'`) en diff de style (`{opacity:{from,to}, x:{from,to}, ...}`) + `triggerMs` + `durationMs`, pour `intro` et `outro`.
4. Le Builder déclare une action nommée par transition, directement dans `perso.actions` — jamais dans l'eventime :
   ```ts
   // sur le perso — `ease` omis ici : illustre le cas où la transition résolue n'en porte pas (cf point 5, aucune valeur ajoutée par le Builder) :
   actions: {
     'item-1-intro': { style: {
       opacity: { to: 1, duration: 400 },
       x: { to: 0, duration: 400 },
     } },
     'item-1-outro': { style: {
       opacity: { to: 0, duration: 400 },
     } },
   }

   // sur story.eventimes — pur déclencheur, aucune donnée :
   { name: 'item-1-intro', startAt: resolvedIntro.triggerMs }
   { name: 'item-1-outro', startAt: resolvedOutro.triggerMs }
   ```
   **Nom d'action unique par perso, obligatoire** : un event route vers *tout* perso dont `actions` porte une clé de ce nom — un nom générique comme `'intro'` réutilisé identiquement sur plusieurs persos ferait déclencher les mauvais items en même temps (collision inter-perso). Convention proposée : `${persoId}-intro`/`${persoId}-outro` (pas une exigence Codplay, juste une convention Builder à tenir).
5. **`ease` n'est pas fourni par capsule-automation aujourd'hui** (`AutoCapsuleEventDefinition.style` ne porte que `{from?,to}`, pas d'easing) — facultatif, absent en sortie s'il est absent en entrée. Application directe du Principe B : si un besoin réel de valeur par défaut apparaît, il s'ajoute au catalogue capsule-automation (`DEFAULT_AUTO_CAPSULE_EVENT_DEFINITIONS`) ou en réglage éditeur, jamais comme constante dans le Builder.

### 7. Feuille de style

`AutoCapsule.renderStyleSheet()` → chaîne CSS → `new Blob([css], { type: 'text/css' })` → `URL.createObjectURL(blob)` → enregistré via :
```ts
CodPlay.load({ scene, mountTarget, extraResources: [{ url: blobUrl, type: 'css', policy: { cache: 'no-store' } }] })
```
**Jamais via `BuilderFacade.compile()` directement** — ce niveau n'a aucun paramètre pour des ressources supplémentaires (confirmé, cf audit). Le Builder ed2 doit donc appeler `CodPlay.load()`, pas `BuilderFacade.compile()` + `Player.init()` séparément — sauf si l'éditeur a une raison précise de vouloir compiler sans monter (à évaluer si le besoin se présente).

### 8. Construction du `SceneDef` — via `SceneDocEditor` remis à niveau

**`SceneDocEditor` est conçu pour cet emploi — c'est lui qui devient la classe métier testable du Builder, pas un helper qu'on contourne avec des littéraux à la main.** Sa remise à niveau (id explicite optionnel, `trackId`/`state`/`onStart`/`onSequenceEnd`/`list` conservés) est traitée comme son propre chantier, préalable à celui-ci — cf `2026-07-08-scenedoceditor-audit-plan.md` (le décalage entre `SceneDocEditor` et le modèle Codplay actuel est confirmé par git, pas seulement supposé : `Perso`/`PersoDoc` ont divergé depuis la séparation des deux classes).

Une fois `SceneDocEditor` remis à niveau, le Builder construit la scène **exclusivement via son API** (`createStory`/`upsertStory`, `createPerso`/`upsertPerso`, les setters `scene.*`) — pas de littéraux `SceneDef`/`Perso` à la main, pas de contournement post-`exportSceneDoc()`. Reste à respecter dans tous les cas :
- Poser `move` explicitement sur chaque perso — l'omission résout à `'@root'` par défaut, jamais à « pas de montage » (il faut `move:'@off'` explicite pour ça).
- Poser `actions` avec les vraies actions nommées de chaque perso (§6, Principe A) — jamais le raccourci `actions:{[id]:null}`.

---

## Partie plan — ordre d'implémentation

1. ~~**Extension `EditorScene`** (sequence-editor) : champ `rootDecorId` (§4).~~ **Fait (2026-07-09)**.
2. ~~**Chantier séparé, préalable : `2026-07-08-scenedoceditor-audit-plan.md`**~~ **Fait (2026-07-09)**.
3. ~~**Builder minimal, un seul item**~~ **Fait et vérifié par un rendu réel (2026-07-09)** — `packages/editor/src/builder/build-scene.ts` (`buildSceneDoc`), fixture `EditorScene` → `SceneDocEditor` → `SceneDef`, un item texte enfant de la capsule racine, transition `fade` nommée statiquement (Principe A), sans `CapsuleDistribution`/capsule-automation (prévu à l'étape 5). Testé (5 tests dont un compile réel via `BuilderFacade`) **et rendu dans une démo** (`?demo=ed2-builder`, `packages/demos/src/scenes/ed2-builder-scene.ts` + `codplay/ed2-builder-demo.ts`) — décision explicite de l'utilisateur de valider par un rendu visible, pas seulement par des tests automatisés. `packages/demos` a gagné une dépendance sur `@codplay/editor` (nouvel alias générique étendu à `packages/editor` en plus de `packages/authoring/*`, cf `build/resolve-codplay-authoring.ts` et `tsconfig.json` — bug latent trouvé et corrigé au passage, jamais rencontré avant : le résolveur ne testait jamais l'extension `.ts` pour un sous-chemin explicite, seulement pour le fallback `index.ts`).
4. ~~**Validation feuille de style par un test concret**~~ **Fait, vérifié en démo uniquement — pas par un test automatisé (2026-07-09)**. `buildSceneDoc()` retourne maintenant `{sceneDoc, styleSheet}`. **Vérifié empiriquement que jsdom ne peut pas prouver ce mécanisme** : sondage jetable (`link.onload` sur une URL `blob:` d'une feuille CSS, puis `fetch()` sur la même URL) — aucun des deux n'aboutit dans l'environnement de test jsdom du repo, donc un test automatisé bout-en-bout ici ne prouverait rien. Câblé dans la démo (`ed2-builder-demo.ts` : `Blob` → `URL.createObjectURL` → `extraResources`) — seule preuve concrète possible pour ce mécanisme précis, confirmée visuellement par l'utilisateur.
   - **Complément Codplay-core (même jour, hors périmètre strict du Builder mais découvert par cette démo)** : `preload.load()`/`loadCss` (`packages/codplay/src/preload/`) injectaient toute feuille de style `type:'css'` dans `document.head`, globalement — remarque de l'utilisateur : « pour Codplay, le style scopé doit apparaître dans le container de la scène... pour ne pas polluer le reste de la page. » `PreloadOptions.container` ajouté, transmis par `player.init()` (déjà propriétaire du `mountTarget` à cet instant) ; `loadCss` scope désormais via `@scope (...)` — CSS moderne, choix explicite de l'utilisateur (alternative : préfixage de sélecteurs). Sans `container` (préchargement outillage avant montage), retombe sur l'ancien `<link>` global inchangé.
   - **Correction importante en cours de route** : une première tentative faisait porter le `<style>` scopé par `mountTarget` lui-même (directement, puis via un wrapper `container-inner`) — **violation d'un invariant documenté** (`docs/formalisation/v1-invariants.md`, « Invariants moteur » : « le moteur ne crée jamais de node lui-même... Codplay doit pouvoir jouer une scène entièrement en canvas, sans DOM »), confirmée par un test réel existant (`mounted-stories-runtime.spec.ts`, verrouille que `mountTarget.children` sont exactement les persos racines authored, rien d'autre). Revenu en arrière avant de livrer : le `<style>` scopé vit dans `document.head` comme n'importe quelle feuille de style — `@scope` fonctionne par correspondance de sélecteur, pas par position physique dans le DOM — seul un **attribut** (`data-codplay-scope`, une métadonnée sur un élément déjà fourni par l'appelant, pas un node ajouté) est posé sur `mountTarget` pour ancrer le sélecteur.
   - Affecte tous les appelants de `extraResources`/`type:'css'` dans le repo (`preload-media-demo.ts` en plus d'`ed2-builder-demo.ts`) — testé (5 cas, `packages/codplay/tests/preload/load-css.spec.ts`, `fetch` mocké puisque jsdom ne peut pas non plus le vérifier bout-en-bout sur une URL `blob:`).
5. ~~**Branchement `CapsuleDistribution` + capsule-automation**~~ **Fait, vérifié (2026-07-09)**. L'eventime posé à la main (étape 3) est remplacé par le pipeline complet : `CapsuleDistribution.compute()` (timing du/des enfant(s) de la capsule racine) → `AutoCapsule` (grille/placement/transitions/CSS, capsule racine en type `card`) → eventimes + actions, toujours construits via `SceneDocEditor`. Zone fantôme (§3/§11 de la spec capsule) calculée après coup depuis la grille réellement résolue (`buildGrid()` puis `setChildPlacement()`), jamais un empan codé en dur. `styleSheet` vient maintenant de `AutoCapsule.resolve().styleSheet` (plus de règle CSS écrite à la main) — la grille/le placement ne sont **jamais** dupliqués en style inline, uniquement via la classe générée + la feuille injectée, pour que la démo reste une vraie preuve du mécanisme (une régression du Blob CSS se verrait). La transition (`ref`, `durationMs`) vient désormais de `Keyframe.transitionIn`/`.transitionOut` — jamais d'un défaut posé par le Builder au niveau capsule (Principe B) ; `card` garde son propre défaut de type (`fade`/`fade`) comme seul filet, pour un enfant qui n'aurait pas de transition nommée sur sa keyframe.
6. ~~**Capsule imbriquée**~~ **Fait, vérifié (2026-07-09)**. `build-scene.ts` remplace la boucle plate (racine → enfants) par un worklist (file, pas récursion sur la pile d'appel) : chaque niveau de capsule (racine ou imbriquée) résout sa propre grille/timing via `resolveCapsule(tracks, capsuleType)` — généralisation de l'ancien `resolveRootCapsule`, la zone fantôme (§3/§11) restant réservée au type `card` (seul type `explicitOnly` ; les autres résolvent leur placement automatiquement). `CapsuleKind`/`AutoCapsuleType` sont les mêmes chaînes littérales (`carousel`/`rangee`/`liste`/`grille`/`card`, vérifié dans le registre capsule-automation), pas de table de traduction.
   - **Précédent manquant, trouvé avant de coder plus loin (remarque utilisateur : « attention à ne pas inventer de structures parce qu'elles ont l'apparence de quelque chose de familier »)** : une recherche dans les démos/tests a confirmé que le nesting `list`-dans-`list` existe (`chrono-story.ts`, `chrono-root → chrono-wrapper → chrono-needle/dot`) mais uniquement comme wrapper inerte (`actions:{}`) ; qu'un perso auto-transitionnant tout en parentant des enfants existe aussi (`quiz-question-panel`, `s4-quiz-reference-scene.ts`) mais son parent est un outlet layout, pas un autre `list` ; et qu'**aucune démo ni test ne combine les deux** (un `list` non-racine, parenté sous un autre `list` non-racine, qui auto-transitionne ET parente ses propres enfants). Cette combinaison est exactement ce que le Builder construit pour une capsule imbriquée.
   - **Décision utilisateur** : une capsule imbriquée est traitée exactement comme n'importe quel autre enfant — elle reçoit sa propre transition intro/outro si elle n'en a pas définie explicitement (résolution normale capsule-automation, pas un cas spécial). Un futur helper éditeur pourra supprimer une transition sur la timeline (feature à ajouter plus tard), mais ce n'est pas résolu en interne dans le Builder — confirmé cohérent avec le code déjà écrit (`buildNestedCapsulePerso` appelle `resolveTransitionActions`, exactement comme `buildItemPerso`, extrait en fonction commune — pas de duplication ni de cas spécial "les capsules ne transitionnent jamais").
   - **Précédent testé isolément avant d'y faire confiance** : nouveau test, `packages/codplay/tests/v1/nested-list-self-transition.spec.ts` — un perso `list` parenté sous un autre `list` non-racine, portant sa propre action de transition (`{from,to,duration}`), joué via le vrai pipeline (`BuilderFacade.compile()` → `Player.init()` → `player.emit()`). Première tentative avec l'adaptateur d'animation réel (anime.js) a échoué : la valeur `to` n'est jamais écrite directement sur le style (branche DOM synchrone `skipTransitionValues:true`), seule l'interpolation réelle d'anime.js l'atteint, ce qui n'a pas le temps de se produire avec un simple `setTimeout(0)` dans jsdom. Corrigé en suivant la convention déjà établie dans le repo (`reference-scenes.spec.ts`'s `createApplyingAnimeImplementation`) : `animationAdapter` factice injecté au `Player`, qui applique `to` immédiatement — précédent maintenant réel et testé, pas supposé.
   - **Rendu en démo (`?demo=ed2-builder`), sur demande explicite** : `ed2-builder-scene.ts` reconstruit avec la fixture 2 niveaux (root card → capsule-a grille avec fade → [capsule-b liste → item-nested (jaune), item-flat (bleu)]), décors distincts par item pour que le nesting soit vérifiable à l'œil, pas seulement par le code — remplace la fixture step-3 (un seul item plat), même route/fichier que chaque incrément précédent.
   - **Débordement découvert par l'utilisateur en testant la démo, corrigé** : `story-main__root` débordait de `#demo-container` (`mountTarget` lui-même, pas un enfant à l'intérieur — `run-codplay-scene-demo.ts`). Cause tracée dans le code, pas supposée : `buildAutoCapsuleGrid` (`packages/authoring/capsule-automation/src/core/build-grid.ts`) ne génère jamais de `width`/`height`, seulement `display:grid`+template — la capsule racine avait `width:100%;height:100%` posé À LA MAIN en style inline (contredisant son propre commentaire, « la grille ne vient QUE de la classe résolue »), tandis que `buildNestedCapsulePerso` n'en posait aucun du tout — donc `capsule-a` (type `grille`, sans dimension bornée) grandissait selon son contenu, débordant du cadre.
   - **Nouveau concept dédié dans capsule-automation, sur arbitrage utilisateur** : `AutoCapsuleDefinition.sceneRoot?: boolean` — marque la capsule qui fait le pont scène↔page (jamais une capsule imbriquée, qui se dimensionne toujours depuis sa propre grille/contenu). Nommage `sceneRoot` choisi pour matcher le vocabulaire déjà établi ailleurs dans ce repo (`sceneRoot: Element`, `packages/authoring/selection-frame`). **Corrigé une première fois par l'utilisateur** : ma première implémentation mélangeait la dimension (`width`/`height`) dans le nom/la règle de la classe grid elle-même (`ac-grid-...-scene-root`) — deux préoccupations CSS distinctes (position/structure de grille vs. dimension) ne doivent jamais partager la même classe. Corrigé : classe fixe séparée `ac-scene-root` (`width:100%;height:100%`), ajoutée à côté de la classe grid, jamais fusionnée dedans — le nom de la classe grid reste identique avec ou sans `sceneRoot` (vérifié par test). Préfixe `ac-` respecté (convention déjà suivie par toutes les classes générées du package).
   - **Second rappel utilisateur, respecté par construction** : « le style inline est réservé aux transitions de l'animation par principe » — `sceneRoot` n'a jamais utilisé `inlineStyle`/`style`, seulement `className`/`cssRules` (comme la grille). Le style du décor d'auteur (`EditorScene.decors[...].data.style`) reste hors périmètre de cette règle pour l'instant — piste notée pour plus tard : distinguer par perso ce qui est structurellement statique (→ classe) de ce qui peut varier (→ style), ce qui pourrait aussi aider Codplay à discriminer ses propres valeurs dynamiques des statiques.
   - **Correction supplémentaire, signalée par l'utilisateur après vérification** : « w et h ne suffisent pas, le parent est grid, alors grid-area doit être 1/-1. » `#demo-container` (le vrai conteneur hôte, `mountTarget`) est lui-même `display:grid` (`demo-shell.css`, `place-items:center`, sans `grid-template` explicite) — sans placement explicite, un enfant grid s'auto-place dans UNE SEULE cellule implicite, donc `width:100%;height:100%` ne remplissait que cette cellule, pas la surface totale du parent. `.ac-scene-root` porte désormais aussi `grid-area:1/-1` (première à dernière ligne/colonne, peu importe le nombre réel de pistes du parent — contrairement à un placement de zone fantôme, aucune connaissance du conteneur externe n'est nécessaire ici).
   - **Deuxième correction, signalée après revérification** : « ac-scene-root déborde encore, car ses enfants eux-mêmes débordent. » Cause : le défaut CSS Grid `min-width:auto`/`min-height:auto` sur un enfant de grille — il refuse de rétrécir sous la taille intrinsèque de son propre contenu, ce qui pousse la piste implicite du parent (`auto`, faute de `grid-template` explicite) à grandir pour l'accueillir ; `width:100%;height:100%` ne bornait donc rien réellement, la piste grandissait autour du contenu au lieu que le contenu soit contraint à la piste. `.ac-scene-root` porte désormais aussi `min-width:0;min-height:0` (force la contrainte à s'appliquer) et `overflow:hidden` (filet de sécurité final une fois la taille effectivement bornée).
   - **Confort visuel, une fois le débordement résolu** : « les éléments doivent occuper plus qu'une cellule, ils sont trop petits. » Cause confirmée dans `resolve-placement.ts` : le placement automatique (`buildAutoPlacement`) pose toujours `rowSpan:1/colSpan:1` par enfant — sur `capsule-a` (type `grille`, défaut 9×16) avec seulement 2 enfants réels, chacun occupait 1/144e de la surface. Corrigé côté fixture (choix explicite de l'utilisateur — démo temporaire, pas de vrai champ auteur pérenne pour l'instant) : nouveau champ **provisoire** `TrackNode.grid?: {rows?, cols?}` (documenté comme temporaire, un futur panneau `CapsulePatch.grid` le remplacera), branché dans `resolveCapsule`/`buildNestedCapsulePerso` (jamais un défaut inventé — absent en entrée reste absent, Principe B), posé à `{rows:1, cols:2}` sur `capsule-a` dans `ed2-builder-scene.ts` (2 enfants réels, chacun remplit désormais la moitié).
   - **Bug de rendu signalé après vérification : `capsule-b` reste figée à `opacity:0`.** Cause trouvée par sondage direct (test jetable inspectant la sortie exacte du Builder, pas une supposition) : `capsule-b` n'avait qu'UN SEUL keyframe dans la fixture démo (`kf-b-intro`, `timeMs:0`) — contrairement à tous les autres tracks (2 keyframes, intro/outro). `CapsuleDistribution` calcule alors `lockedIntroMs = lockedOutroMs = 0` (même keyframe pour les deux bornes), donc son eventime outro (`opacity:1→0`) se déclenche exactement au même tick que son intro (`opacity:0→1`) — l'annulant avant qu'elle ait pu progresser. Pas un bug du Builder ni de capsule-automation : une incohérence de la fixture démo elle-même. Corrigé en ajoutant le keyframe outro manquant (`timeMs:3000`, symétrique aux autres tracks) — revérifié par le même sondage que l'eventime outro se résout bien à `t=3000`, séparé de l'intro.
   - **Analyse plus profonde demandée : « maintenant c'est item-b [nested] qui s'affiche, mais plus item-flat »** — deuxième bug distinct, plus structurel. Sondage direct a montré `item-flat-intro` résolu à `startAt:3000` (pas `0`) alors que ses propres keyframes sont symétriques (`0`/`3000`, comme tout le monde). Cause : `resolveCapsule` code en dur `mode:'sequential'` pour TOUTE capsule — ce mode fait délibérément avancer un curseur temporel partagé entre enfants (`computeSequentialForward`, §4 de la spec capsule-distribution, comportement voulu et documenté pour ce mode). Avec `capsule-b` ET `item-flat` tous deux entièrement lockés `0→3000`, le premier « consommait » toute la durée et repoussait le second à `[3000,3000]` (invisible). **Recherche remontée jusqu'à Eddy** (sur indication explicite de l'utilisateur — « les descriptions des capsules ont été faites dans Eddy ») : son `TIME_MODE.distributed` (défaut de TOUS les types, y compris `carousel`) traite les enfants verrouillés de façon INDÉPENDANTE — chacun garde sa plage fixe, sans curseur partagé ; seuls les enfants libres se répartissent les segments restants. **Ce mode n'a jamais été porté lors de la réconciliation capsule-automation** — seul `sequential` a survécu (sous ce nom, correspondant en fait à l'ancien comportement `carousel`-only d'Eddy), laissant `CapsuleDistribution` sans équivalent du défaut universel `distributed`.
   - **Fausse piste, corrigée avant livraison — « accumulation » abandonné** : première tentative (spec §3.2 + `computeAccumulation`, `CapsuleDistribution` recevant `capsuleType` directement) rejetée par l'utilisateur : « je pense que "accumulation" est une erreur, un concept que tu as introduit récemment, et qui ne tient pas... sequential et stagger disent... si les items se suivent l'un après l'autre, ou bien s'ils apparaissent avec un délai de décalage. ce sont les seuls paramètres de temporalité. » La coexistence voulue n'est pas un 3e mode : c'est la conséquence de `stagger` avec `staggerInMs:0`/`staggerOutMs:0`, combiné à une grille à plusieurs cellules (déjà latent dans `computeStagger`, jamais exploité par mon défaut). `computeAccumulation` entièrement retiré, spec §3.2 supprimée, `CapsuleDistribution` revenue à `mode` toujours obligatoire — exactement sa forme d'avant toute intervention de ce chantier.
   - **Deuxième recadrage, sur la frontière de responsabilité** : « les sous-types de capsules doivent être résolus dans le sequence-editor. point. » et « la résolution type de capsule vers distribution doit être une classe métier, distincte ou fusionnée dans les autres. » `CapsuleDistribution` ne connaît plus AUCUN `CapsuleKind`/`capsuleType` — nouvelle classe métier dédiée, **`CapsulePreset`** (colocalisée `packages/authoring/scene-factory/`, aux côtés de `CapsuleDistribution`/`SceneDocEditor`/le futur moteur de validation), résout `CapsuleKind` + réglage d'auteur (`TrackDistribution`) → `{mode, staggerInMs?, staggerOutMs?}` prêt pour `CapsuleDistribution.compute()`. Seul `carousel` a un défaut structurel réel (`sequential` — sa grille forcée 1×1 n'admet aucune alternative) ; tout autre sous-type EXIGE un réglage explicite (`CapsulePreset.resolve()` lève sinon) — aucune table de défauts par type devinée pour `rangee`/`grille`/`card`/`liste`, une future interface proposera ses propres presets (ex. « ligne = 1×3, stagger:2 »), jamais une constante figée ici. `sequence-editor` (`machine.ts`) ET le Builder (`resolveCapsule`) appellent tous deux `CapsulePreset`, plus aucune table dupliquée côté appelant.
   - **`stagger:0` = no-op transparent, par construction** : « le mécanisme "stagger:0" est censé être optimisé dans le builder qui ne doit pas produire un "décalage à 0" mais l'ignorer. le mettre ou non ne doit pas produire d'effet ni de code différent. » Vérifié déjà vrai mathématiquement (`computeStagger` : un enfant libre avec `staggerInMs:0` résout à `introMs:0` ; un enfant pleinement locké retourne toujours sa plage exacte peu importe le stagger) — la capsule racine (jamais authored, aucun `TrackDistribution` propre) reçoit `{mode:'stagger', staggerInMs:0, staggerOutMs:0}` posé explicitement par le Builder comme choix structurel de son rôle, documenté comme tel, pas un défaut deviné depuis `card`.
   - Résultat : 13 tests `CapsuleDistribution` (inchangés, `mode` toujours explicite), 6 tests `CapsulePreset` (`packages/editor/tests/capsule-preset.spec.ts`, dont le cas exact du bug via l'intégration réelle `CapsulePreset.resolve()` → `CapsuleDistribution.compute()`), 28 tests `packages/authoring/capsule-automation`, 243 tests `packages/editor`, 285 tests `packages/codplay` (inchangé), typecheck propre, démo `?demo=ed2-builder` répond 200, **confirmé visuellement par l'utilisateur** (« oui, tout fonctionne ») — `capsule-b` ET `item-flat` coexistent désormais correctement via un réglage d'auteur explicite (`{mode:'stagger', staggerInMs:0, staggerOutMs:0}`) posé sur `capsule-a`/`capsule-b` dans la fixture, pas un défaut deviné.
   - **Audit de nettoyage, demandé explicitement puis mené (2026-07-09)** : « tu feras ensuite un nettoyage pour ne pas disperser des définitions de capsules un peu partout dans le code... des classes métier testées et isolées, les systèmes d'état les consomment. » Agent d'exploration dépêché sur tout le repo — 2 points réels trouvés, le reste confirmé légitime (voir rapport complet, non reproduit ici : `CAPSULE_TYPE`/capsule-automation reste délibérément découplé, `AUTO_CAPSULE_TYPE_BEHAVIORS` a 33 tests et un seul point de lecture par fichier, `selection-frame` ne consomme que des types résolus, `TrackDistribution` est un miroir de frontière volontaire, `CapsulePatch.sequencing` de dedit est du code mort déjà signalé stale par la spec).
     - **Point 1, corrigé** : `build-scene.ts` testait `capsuleType === CAPSULE_TYPE.card` pour décider quand poser la zone fantôme — redérivait un fait déjà résolu ailleurs (`AUTO_CAPSULE_TYPE_BEHAVIORS.card.placementPolicy === explicitOnly`) via une égalité littérale, silencieusement caduque si un futur type recevait aussi `explicitOnly`. **Rappel de principe de l'utilisateur en cours de correction** : « pour que le builder soit solide et pérenne, on doit éviter au maximum des tests discrets sur une situation... on est sur du déclaratif au maximum » — ma première réponse (`AutoCapsule.requiresExplicitPlacement(): boolean`, appelée via un `if` dans le Builder) restait un test discret, juste mieux informé. Précisé par l'utilisateur : « résoudre tous les cas du Builder... via des classes métier testables. le Builder dépend souvent d'une chaîne de traitements, il faut isoler au maximum chaque traitement. » Corrigé à la vraie source : `resolveAutoCapsulePlacement` (`capsule-automation/src/core/resolve-placement.ts`) génère désormais elle-même la zone fantôme pour tout enfant sans placement dans un type `explicitOnly` (diagnostic `info` `placement-ghost-zone-applied`, plus de warning) — **aucun `if` sur le sous-type ne reste dans le Builder**, `AutoCapsule.resolve()` produit le bon résultat par construction, pour n'importe quel type `explicitOnly` présent ou futur. `requiresExplicitPlacement()` retirée (mauvaise couche, superflue une fois la vraie source corrigée). `build-scene.ts` : `buildGhostZonePlacement`/la boucle `setChildPlacement` conditionnelle entièrement supprimées.
     - **Point 2, laissé tel quel sur choix explicite** : `carousel-scene.ts` (démo) recalcule à la main une répartition séquentielle déjà documentée comme pis-aller pré-Builder — décision de l'utilisateur de ne pas migrer maintenant (pas un vrai risque de duplication dangereuse, un futur chantier à part si besoin).
     - Résultat : 1 test `resolve-placement.spec.ts` corrigé pour refléter le nouveau comportement voulu (placement plein-cadre + diagnostic `info`, plus jamais placement vide + `warning`) — 28/28 `capsule-automation` inchangé en nombre, 243/243 `editor`, 285/285 `codplay`, démo répond 200.
7. ~~**Tests**~~ **Fait (2026-07-09)** — demande explicite : « on termine les tests builder, avec un résumé intelligible de ce qu'il fait et comment. » Couverture ajoutée pour les 3 chemins de rejet jamais testés jusqu'ici (Principe B — le Builder ne devine jamais, il lève) : capsule sans `capsuleType`, `contentType` non supporté, capsule non-`carousel` sans `distribution` explicite (`CapsulePreset` lève). Ajouté aussi : une capsule imbriquée (pas seulement la racine) avec son propre `TrackNode.grid` override, jamais vérifié explicitement au niveau du Builder (seulement exercé à la main dans la démo) — confirme que la classe générée résout bien `1×2`, pas le défaut `9×16` du type. `build-scene.ts` gagne un long commentaire en tête de `buildSceneDoc()` — vue d'ensemble du pipeline (forme d'un `EditorScene`, les 3 stades de résolution par capsule, comment l'arbre est parcouru sans récursion sur la pile d'appel, les deux sorties) pour un lecteur qui découvre le fichier, sans avoir à reconstruire ce modèle depuis les commentaires locaux dispersés.
   - Résultat : 18 tests `build-scene.spec.ts` (14 existants + 4 nouveaux), 247 tests `packages/editor` (243+4), typecheck propre.

Chantier Builder — **étapes 1 à 7 toutes closes**.

Chantier « cœur » — avancer par incréments courts, montrer le code au fil de l'eau.
