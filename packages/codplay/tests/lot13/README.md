# Lot 13 tests

This folder contains the tests for:

- `L13-T1` idempotent `init/destroy` lifecycle
- `L13-T2` deterministic `play/pause/seek/rewind` state transitions
- `L13-T3` explicit command rejection codes on invalid states/policy
- `L13-T4` command trace and state change subscriptions

Reference:

- `evolution/lots/lot-13-create-player-api-state.md`
