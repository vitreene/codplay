# Lot 05 tests

This folder contains the tests for:

- `L5-T1` transition derivation forwards arbitrary style properties
- `L5-T2` runtime bridge forwards html-style properties to adapter
- `L5-T3` runtime bridge supports non-html object targets

Reference:

- `evolution/lots/lot-05-animation-properties-extensibility.md`
