# Lot 15 tests

This folder contains the tests for:

- `L15-T1` adapts Eddy `persos[]` into converter-compatible records
- `L15-T2` normalizes empty `eventtimes` in preview mode
- `L15-T3` keeps strict converter failure when preview normalization is disabled
- `L15-T4` converts provided manual Eddy snapshot into a player scene

Reference:

- `evolution/lots/lot-15-adaptation-script-eddy-manuel.md`
