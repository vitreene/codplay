/** Default scene-level track always present in the runtime registry. */
export const TRACK_GLOBAL_ID = 'global' as const

/** Prefix used for tracks dedicated to one declared strap. */
export const TRACK_STRAP_PREFIX = 'strap' as const
