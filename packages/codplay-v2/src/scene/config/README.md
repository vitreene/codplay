# Scene Configuration

> Status: En cours
> CodPlay version: V2 foundation

This folder contains named scene validation and product configuration values.
Rules consume this configuration instead of embedding domain paths or defaults
inside validation engines.
