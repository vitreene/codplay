# Scene Configuration

> Status: En cours
> CodPlay version: V2 foundation

This folder contains named scene validation and product configuration values only.
It must not contain payload factories, path resolvers, validators, or other
executable helpers. Rules consume these constants instead of embedding domain
paths or defaults inside validation engines.
