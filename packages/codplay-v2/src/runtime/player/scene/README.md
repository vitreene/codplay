# Player scene reconstruction

> Status: Fixe
> CodPlay version: V2 foundation

This folder contains the pure scene reconstruction used by Play, Seek and the
runner geometry boundary. It preserves the single
`materialize -> resolve -> solve` path and receives player-owned dependencies
explicitly.
