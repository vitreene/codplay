# Tag component V2

> Status: Fini
> CodPlay version: V2 foundation

The author profile is declared in `tag-types.ts`, validated and defaulted in
`tag-validation.ts`, and projected by `tag-component.ts`. The builder makes the
`div` default explicit before the component is instantiated.
