# Layout component V2

> Status: Fini
> CodPlay version: V2 foundation

The author profile is declared in `layout-types.ts`, the markup contract is
validated in `layout-validation.ts`, and the component only projects the
compile-sanitized template and root services.
