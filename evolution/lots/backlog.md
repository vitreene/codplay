# Backlog lots

Lots apres la phase 1 (a ordonner lors de la prochaine revue):

- animation properties extensibility (replace hardcoded simple subset)
- wait flow complet (`startWait/resolveWait` runtime)
- plugin list complet (`diff + FLIP + fallback perf`)
- trace/debug retention + export
- conflits same-tick runtime (application effective des regles)
- media sync avancee + master switching complet
- convertisseur legacy outillage
